<h2>Edit search key</h2>

<%= render "form.html", changeset: @changeset,
                        action: search_key_path(@conn, :update, @search_key) %>

<%= link "Back", to: search_key_path(@conn, :index) %>
